<UserControl x:Class="KFA.MyBlogWPF.Views.Article.ArticlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KFA.MyBlogWPF.Views.Article"
             xmlns:components="clr-namespace:KFA.MyBlogWPF.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20 10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="Articles" 
                       Style="{StaticResource PageHeader}" />
            <Button 
                    Grid.Column="1" 
                    Content="Add"
                    FontSize="16" 
                    Command="{Binding AddArticlesCommand}"/>
            </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <components:ArticlesListing Grid.Column="0" DataContext="{Binding ArticlesListingViewModel}" />
            <components:ArticlesDetails Margin="10 0 0 0" Grid.Column="1" DataContext="{Binding ArticlesDetailsViewModel}" />

        </Grid>
    </Grid></UserControl>
